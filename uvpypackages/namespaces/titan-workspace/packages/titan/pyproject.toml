[project]
name = "titan"
version = "0.0.1"
readme = "README.md"
license = "MIT"
authors = [{ name = "Pedro Tenório" }]
dependencies = []

[project.scripts]
titan = "titan.mod_titan:func_titan"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ['./src/titan']

[tool.ruff]
extend = '../../pyproject.toml'

[tool.pyright]
extends = '../../pyproject.toml'